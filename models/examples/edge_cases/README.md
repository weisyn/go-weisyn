# 边缘情况测试模型

---

## 📌 版本信息

- **版本**：1.0
- **状态**：stable
- **最后更新**：2025-11-12
- **最后审核**：2025-11-12
- **所有者**：AI模型管理组
- **适用范围**：WES 项目中边缘情况测试模型相关功能

---

## 📍 组件定位

**边缘情况测试模型** - 用于测试 WES 平台处理特殊情况和边界条件的能力，包括特殊网络结构、不同数据类型、动态形状、文件名编码等。

## 模型列表

### 1. big_fanout

**模型文件**: `example_big_fanout.onnx`

**生成脚本**: `generate_big_fanout.py`

**描述**:
- 大扇出网络结构
- 测试并行计算能力
- 包含 100 个并行矩阵乘法操作
- 验证 WES 平台对并行化网络的处理

**网络结构**:
- 输入: `[1, 4]` float32
- 输出: `[1, 4]` float32
- 100 个并行 FC 层，然后合并求和

**使用场景**:
- 测试并行计算性能
- 验证大扇出网络处理
- 测试内存管理

**重新生成**:
```bash
cd big_fanout
python generate_big_fanout.py
```

**依赖**:
- torch
- onnx

---

### 2. big_compute

**模型文件**: `example_big_compute.onnx`

**生成脚本**: `generate_network_big_compute.py`

**元数据修改脚本**: `modify_metadata.py`

**描述**:
- 大计算量网络
- 测试复杂计算处理能力
- 包含修改后的元数据（用于测试元数据处理）

**元数据**:
- `doc_string`: "This is a test description."
- `model_version`: 1337
- `domain`: "test domain"
- 自定义元数据属性

**使用场景**:
- 测试复杂计算处理
- 验证元数据处理
- 测试模型版本管理

**重新生成**:
```bash
cd big_compute
python generate_network_big_compute.py
python modify_metadata.py  # 修改元数据
```

**依赖**:
- torch
- onnx

---

### 3. zero_dim_output

**模型文件**: `example_0_dim_output.onnx`

**生成脚本**: `generate_0_dimension_output.py`

**描述**:
- 零维输出模型
- 测试标量输出处理
- 验证 WES 平台对零维张量的支持

**输出特点**:
- 输出维度为 0（标量）
- 测试边界条件处理

**使用场景**:
- 验证标量输出处理
- 测试零维张量支持
- 验证边界条件

**重新生成**:
```bash
cd zero_dim_output
python generate_0_dimension_output.py
```

**依赖**:
- torch
- onnx

---

### 4. dynamic_axes

**模型文件**: `example_dynamic_axes.onnx`

**生成脚本**: `generate_dynamic_axes_network.py`

**描述**:
- 动态轴模型
- 测试动态形状处理
- 验证 WES 平台对动态输入大小的支持

**动态特性**:
- 输入形状可以变化
- 测试运行时形状推断

**使用场景**:
- 验证动态形状处理
- 测试形状推断能力
- 验证可变输入大小

**重新生成**:
```bash
cd dynamic_axes
python generate_dynamic_axes_network.py
```

**依赖**:
- torch
- onnx

---

### 5. float16

**模型文件**: `example_float16.onnx`

**生成脚本**: `generate_float16_network.py`

**描述**:
- Float16 精度模型
- 测试半精度浮点数支持
- 验证 WES 平台对 Float16 数据类型的处理

**数据类型**:
- 使用 Float16（半精度浮点）
- 测试精度转换和兼容性

**使用场景**:
- 验证 Float16 支持
- 测试精度转换
- 验证半精度处理

**重新生成**:
```bash
cd float16
python generate_float16_network.py
```

**依赖**:
- torch
- onnx

---

### 6. odd_name

**模型文件**: `example ż 大 김.onnx`

**生成脚本**: `generate_odd_name_onnx.py`

**描述**:
- 特殊字符文件名模型
- 测试文件名编码处理
- 包含 Unicode 特殊字符（ż, 大, 김）
- 验证文件系统兼容性

**文件名特点**:
- 包含 Unicode 字符
- 测试编码处理
- 验证文件路径处理

**模型功能**:
- 输入: `[1, 2]` int32
- 输出: `[1]` int32（输入求和）

**使用场景**:
- 验证文件名编码处理
- 测试 Unicode 支持
- 验证文件路径兼容性

**重新生成**:
```bash
cd odd_name
python generate_odd_name_onnx.py
```

**依赖**:
- torch
- onnx

---

## 测试流程

### 1. 部署模型

```bash
# 部署大扇出模型
wes ai deploy models/examples/edge_cases/big_fanout/example_big_fanout.onnx \
    --name "Big Fanout Test" \
    --description "Test model for large fanout networks"
```

### 2. 调用模型

```bash
# 使用 JSON-RPC API
curl -X POST http://localhost:28680/jsonrpc \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "wes_callAIModel",
    "params": {
      "private_key": "your_private_key",
      "model_hash": "0x...",
      "inputs": [{
        "name": "input",
        "data": [1.0, 2.0, 3.0, 4.0],
        "shape": [1, 4],
        "data_type": "float32"
      }]
    },
    "id": 1
  }'
```

## 测试覆盖

这些边缘情况测试模型覆盖以下场景：

- ✅ **特殊网络结构**: 大扇出、大计算量
- ✅ **数据类型**: Float16、零维输出
- ✅ **动态特性**: 动态轴、可变形状
- ✅ **文件处理**: Unicode 文件名、特殊字符
- ✅ **元数据**: 自定义元数据属性

## 模型来源

所有模型和生成脚本来自 [onnxruntime_go](https://github.com/yalue/onnxruntime_go) 项目的 `test_data` 目录。

**原始仓库**: https://github.com/yalue/onnxruntime_go

**许可证**: MIT License


