# 🚀 新目录结构快速开始指南

## 📋 重组完成状态

✅ **test目录**: 已重组为单元/集成/E2E测试分类结构  
✅ **configs目录**: 已重组为模板化配置管理系统  
✅ **向后兼容**: 关键符号链接已创建  
✅ **脚本权限**: 所有测试脚本已设置执行权限  

## 🎯 立即可用的功能

### 1. 查看新结构
```bash
# 查看测试目录说明
cat test/README.md

# 查看配置管理说明  
cat configs/README.md
```

### 2. 环境清理（纯净模式）
```bash
# 一键清理所有测试数据
./test/scripts/cleanup/clean-environment.sh
```

### 3. 启动优化的本地节点
```bash
# 使用新的单节点配置
./bin/node --config configs/environments/local/single-node.json

# 或者使用符号链接（保持兼容）
./bin/node --config configs/config.json
```

### 4. 运行自动化E2E测试
```bash
# 纯净模式测试（推荐）
./test/scripts/automation/run-e2e-tests.sh clean

# 继承模式测试（保留数据）
./test/scripts/automation/run-e2e-tests.sh persistent
```

## 📁 新目录结构

### Test目录层次
```
test/
├── unit/           # 单元测试（按组件分类）
├── integration/    # 集成测试（按功能分类）
├── e2e/           # 端到端测试
│   ├── scenarios/ # 测试场景
│   ├── clean/     # 纯净模式测试
│   └── persistent/# 继承模式测试
├── fixtures/      # 测试数据和配置
├── scripts/       # 测试脚本工具
├── docs/          # 测试文档和报告
└── tools/         # 测试辅助工具
```

### Config目录层次
```
configs/
├── templates/     # 配置模板（base/client/miner/relay）
├── environments/  # 环境配置（local/testnet/mainnet）
│   └── local/    # 本地开发环境
│       └── single-node.json  # 单节点配置
├── shared/        # 共享配置片段
└── validation/    # 配置验证工具
```

## 🔧 关键改进

### 配置管理
- **消除重复**: 从4个重复配置 → 1个基础模板 + 3个角色模板
- **环境分离**: 清晰的本地/测试网/主网配置
- **模板继承**: 基础配置 + 环境特化 + 角色优化

### 测试管理  
- **类型分离**: 单元/集成/E2E测试清晰分类
- **数据策略**: 纯净模式 vs 继承模式
- **自动化框架**: 一键环境清理和测试执行

### 开发体验
- **文档完善**: 详细的使用指南和最佳实践
- **工具自动化**: 减少手动操作，提高效率
- **向后兼容**: 现有脚本无需修改

## 🎯 测试策略

### 纯净模式（推荐用于功能验证）
- 每次测试前清空所有数据
- 适用于：新功能验证、性能测试、回归测试
- 命令：`./test/scripts/automation/run-e2e-tests.sh clean`

### 继承模式（用于用户场景模拟）
- 保留历史数据，累积区块链状态
- 适用于：用户体验测试、长期稳定性测试
- 命令：`./test/scripts/automation/run-e2e-tests.sh persistent`

## 📊 配置模板说明

| 模板 | 用途 | 特点 |
|------|------|------|
| `base.json` | 基础配置 | 包含所有标准配置项 |
| `client.json` | 客户端节点 | 优化网络连接，支持移动环境 |
| `miner.json` | 矿工节点 | 启用挖矿API，优化资源使用 |
| `relay.json` | 中继节点 | 提供网络中继服务 |

## ⚡ 快速验证

### 1. 验证配置
```bash
# 检查单节点配置
cat configs/environments/local/single-node.json | jq '.api.http.port'
# 应该输出: 8080
```

### 2. 验证脚本
```bash
# 检查清理脚本
./test/scripts/cleanup/clean-environment.sh --help

# 检查E2E测试脚本  
./test/scripts/automation/run-e2e-tests.sh --help
```

### 3. 验证符号链接
```bash
# 检查兼容性链接
ls -la configs/config.json configs/genesis.json
```

## 🔄 与原有工作流的兼容性

### Scripts目录
- `scripts/build.sh` - 构建脚本正常工作
- `scripts/quick_mining_test.sh` - 挖矿测试正常工作
- `scripts/token_operations.sh` - 代币操作正常工作

### 现有配置引用
- `configs/config.json` → `configs/environments/local/single-node.json`
- `configs/genesis.json` → `test/fixtures/genesis/genesis.json`

## 🎉 立即开始

1. **快速测试**: `./test/scripts/automation/run-e2e-tests.sh clean`
2. **启动节点**: `./bin/node --config configs/config.json`  
3. **查看文档**: `cat test/README.md` 和 `cat configs/README.md`

---

**重组完成时间**: $(date)  
**新结构状态**: ✅ 可立即使用
