# 🔑 密钥生成工具 (Keygen)

> **工具功能**: 生成 WES 区块链密钥对和创世块密钥

## 📋 快速开始

```bash
# 生成单个密钥对
go run ./cmd/tools/keygen generate 1

# 生成多个密钥对
go run ./cmd/tools/keygen generate 5

# 生成创世块密钥
go run ./cmd/tools/keygen genesis
```

## 功能说明

`keygen` 工具用于生成 WES 区块链系统所需的密钥对，包括：

- ✅ 账户密钥对（私钥、公钥、地址）
- ✅ 批量密钥生成
- ✅ 创世块密钥（验证者和初始账户）

### 主要特性

1. **安全随机**: 使用密码学安全的随机数生成器
2. **标准兼容**: 遵循区块链行业标准的密钥格式
3. **批量高效**: 支持批量密钥生成
4. **格式灵活**: 支持多种输出格式

## 使用方法

### 基本用法

```bash
# 编译工具
go build -o bin/wes-keygen ./cmd/tools/keygen

# 查看帮助
./bin/wes-keygen
```

### 命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `generate <count>` | 生成指定数量的密钥对 | `generate 5` |
| `genesis` | 生成创世块密钥文件 | `genesis` |

## 使用示例

### 生成单个密钥对

```bash
go run ./cmd/tools/keygen generate 1
```

输出示例：
```
🔑 生成 1 个密钥对
====================
密钥对 1:
  私钥: 4f3edf983ac636a26c1883063d5b7e5c5b7e6a4c1883063d5b7e5c5b7e6a4c18
  公钥: 024f3edf983ac636a26c1883063d5b7e5c5b7e6a4c1883063d5b7e5c5b7e6a4c18
  地址: CWb1owGnpUaB2JoQPhohpa81Cz9aiqikZG
```

### 生成创世块密钥

```bash
go run ./cmd/tools/keygen genesis
```

生成 `genesis_keys.json` 文件，包含验证者和创世账户的密钥信息。

## 安全建议

1. **生产环境使用**: 在安全的离线环境中生成重要密钥
2. **私钥保护**: 妥善保管私钥，避免在网络中传输
3. **备份策略**: 建立安全的私钥备份和恢复机制
4. **访问控制**: 限制密钥文件的访问权限

```bash
# 设置密钥文件安全权限
chmod 600 genesis_keys.json
```

## 相关文档

- **[tools/README.md](../README.md)** - 工具集总览
- **[param-encoder/README.md](../param-encoder/README.md)** - 参数编码工具

