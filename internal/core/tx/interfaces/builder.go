// Package interfaces provides transaction builder interfaces.
package interfaces

import (
	"github.com/weisyn/v1/pkg/interfaces/tx"
)

// Builder 交易构建器内部接口
//
// 🎯 **职责**：继承公共接口，提供 TX 域内部使用的构建能力
//
// 🔄 **继承关系**：
//   - 继承 tx.TxBuilder 公共接口
//   - 当前无内部扩展方法（未来可根据需要扩展）
//
// 📁 **实现目录**：internal/core/tx/builder/
//
// 💡 **设计说明**：
//   - Builder 是纯装配器，不做 UTXO 选择、费用估算等业务逻辑
//   - Type-state 模式保证构建顺序：Compose → Prove → Sign → Submit
//   - Draft 模式支持渐进式构建（ISPC场景）
type Builder interface {
	// 继承公共交易构建器接口
	tx.TxBuilder

	// 💡 内部扩展方法（暂无，保留接口以便未来扩展）
	// 如需添加内部专用方法（如性能优化、调试辅助等），在此扩展
}
