// Package interfaces provides draft transaction interfaces.
package interfaces

import (
	"github.com/weisyn/v1/pkg/interfaces/tx"
)

// DraftService 交易草稿服务内部接口
//
// 🎯 **职责**：继承公共接口，提供交易草稿的创建、修改、封闭能力
//
// 🔄 **继承关系**：
//   - 继承 tx.TransactionDraftService 公共接口
//   - 当前无内部扩展方法（未来可根据需要扩展）
//
// 📁 **实现目录**：internal/core/tx/draft/
//
// 💡 **设计说明**：
//   - Draft 是 Builder 的辅助工具，支持渐进式构建
//   - 用于 ISPC 场景（合约执行中逐步添加输出）和 Off-chain 场景（用户交互式构建）
//   - Draft.Seal() 后转换为 ComposedTx，进入正式的 Type-state 状态机
type DraftService interface {
	// 继承公共交易草稿服务接口
	tx.TransactionDraftService

	// 💡 内部扩展方法（暂无，保留接口以便未来扩展）
	// 如需添加内部专用方法（如批量操作、性能优化等），在此扩展
}
