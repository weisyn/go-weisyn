# JSON-RPC 协议层（internal/api/jsonrpc）

> **📌 模块类型**：`[X] 实现模块` `[ ] 接口定义` `[ ] 数据结构` `[ ] 工具/其他`

---

## 📍 **模块定位**

　　本模块是 WES 区块链节点的**主协议接口层**，实现符合 JSON-RPC 2.0 规范的区块链标准接口，为 DApp、钱包、区块浏览器提供与 Ethereum 生态兼容的 API 服务。

**解决什么问题**：
- **生态兼容**：支持 web3.js/ethers.js 等主流 SDK 直接使用
- **标准协议**：遵循 JSON-RPC 2.0 和以太坊 RPC 规范
- **完整功能**：覆盖链查询、交易提交、状态查询、订阅等核心功能
- **零信任实现**：接收已签名交易，不处理私钥

**不解决什么问题**（边界）：
- ❌ 不实现业务逻辑（调用 `pkg/interfaces`）
- ❌ 不处理 HTTP 服务器（由上层路由器负责）
- ❌ 不验证交易细节（由 TxVerifier 负责）

---

## 🎯 **设计原则与核心约束**

### **设计原则**

| 原则 | 说明 | 价值 |
|------|------|------|
| **规范至上** | 严格遵循 JSON-RPC 2.0 规范 | 生态兼容 |
| **WES 命名** | 使用 `wes_` 前缀区分原生方法 | 避免与 Ethereum 冲突 |
| **错误码细化** | 区块链特有错误码（TX_FEE_TOO_LOW等） | 客户端友好 |
| **状态锚定** | 查询方法支持 blockParameter | 数据一致性 |

### **核心约束** ⭐

**严格遵守**：
- ✅ **JSON-RPC 2.0 规范**：严格遵循 `id`、`jsonrpc`、`method`、`params` 结构
- ✅ **错误码范围**：标准错误（-32700至-32603）+ WES自定义（-32000至-32099）
- ✅ **零信任**：写操作仅接受 `signedTx`，不接受私钥
- ✅ **状态锚定**：查询方法必须支持 `blockParameter`（height/hash/latest）
- ✅ **订阅限制**：订阅方法（`wes_subscribe`）仅在 WebSocket 上工作

**严格禁止**：
- ❌ **破坏规范**：不得违反 JSON-RPC 2.0 协议
- ❌ **接收私钥**：任何方法不得接受私钥参数
- ❌ **HTTP 订阅**：订阅必须使用 WebSocket，不能用 HTTP
- ❌ **忽略重组**：不得返回不含状态锚点的查询结果

---

## 📊 **核心方法列表**

### **阶段 A 核心集（19个方法）** ✅ 已实现

### **资源查询扩展方法（2个方法）** ✅ 已实现
- `wes_getResourceCode` - 获取资源代码/字节码
- `wes_getResourceABI` - 获取资源 ABI

**详细说明**: 请参考 [资源元数据标准化规范](../docs/jsonrpc_resource_metadata.md)

### **权限管理方法** ✅ 已实现（使用通用交易方法）

权限管理功能通过以下通用 RPC 方法实现：

- `wes_getUTXO` - 查询资源 UTXO（用于获取当前锁定条件）
- `wes_buildTransaction` - 构建权限变更交易（支持权限操作 draft）
- `wes_computeSignatureHashFromDraft` - 计算权限变更交易的签名哈希
- `wes_finalizeTransactionFromDraft` - 完成权限变更交易（添加签名）
- `wes_sendRawTransaction` - 提交权限变更交易

**权限操作类型**（通过 draft.metadata.operation 标识）：
- `transfer_ownership` - 所有权转移
- `update_collaborators` - 协作者管理（MultiKey）
- `grant_delegation` - 委托授权
- `set_time_lock` - 设置时间锁
- `set_height_lock` - 设置高度锁

**详细说明**: 请参考 [权限管理 JSON-RPC 方法说明](../docs/jsonrpc_permission_management.md)

权限管理功能通过以下通用 RPC 方法实现：

- `wes_getUTXO` - 查询资源 UTXO（用于获取当前锁定条件）
- `wes_buildTransaction` - 构建权限变更交易（支持权限操作 draft）
- `wes_computeSignatureHashFromDraft` - 计算权限变更交易的签名哈希
- `wes_finalizeTransactionFromDraft` - 完成权限变更交易（添加签名）
- `wes_sendRawTransaction` - 提交权限变更交易

**权限操作类型**（通过 draft.metadata.operation 标识）：
- `transfer_ownership` - 所有权转移
- `update_collaborators` - 协作者管理（MultiKey）
- `grant_delegation` - 委托授权
- `set_time_lock` - 设置时间锁
- `set_height_lock` - 设置高度锁

**详细说明**: 请参考 [权限管理实施报告](../../workbench/contract-workbench.git/_dev/PERMISSION_MANAGEMENT_IMPLEMENTATION.md)

#### **1️⃣ 链信息（4个）** - `chain.go`
- `net_version` - 返回网络 ID
- `wes_chainId` - 返回链 ID（十六进制）
- `wes_syncing` - 返回同步状态
- `wes_blockNumber` - 返回最新区块高度

#### **2️⃣ 区块查询（2个）** - `block.go`
- `wes_getBlockByHeight` - 按高度查询区块（支持 blockParameter）
- `wes_getBlockByHash` - 按哈希查询区块

#### **3️⃣ 交易方法（4个）** - `tx.go`
- `wes_getTransactionByHash` - 查询交易详情
- `wes_getTransactionReceipt` - 查询交易收据
- `wes_sendRawTransaction` - 提交已签名交易（✅ 细化错误码）
- `wes_estimateFee` - 估算交易费用

#### **4️⃣ 状态查询（3个）** - `state.go`
- `wes_getBalance` - 查询余额（支持 blockParameter）
- `wes_getUTXO` - 查询 UTXO（支持 blockParameter）
- `wes_call` - 模拟合约调用

#### **5️⃣ 交易池（2个）** - `txpool.go`
- `wes_txpool_status` - 查询交易池状态
- `wes_txpool_inspect` - 按地址查询待处理交易

#### **6️⃣ 订阅（2个）** - `subscribe.go`
- `wes_subscribe` - 订阅事件（⚠️ 仅 WebSocket）
- `wes_unsubscribe` - 取消订阅

---

## 📁 **目录结构**

```
jsonrpc/
├── server.go                # ✅ JSON-RPC 服务器
├── README.md                # 本文档
├── methods/                 # ✅ RPC 方法实现（19个方法）
│   ├── chain.go            # 链信息方法（4个）
│   ├── block.go            # 区块查询方法（2个）
│   ├── tx.go               # 交易方法（4个，含错误码细化）
│   ├── state.go            # 状态查询方法（3个）
│   ├── txpool.go           # 交易池方法（2个）
│   ├── subscribe.go        # 订阅方法（2个，仅WS）
│   └── README.md
└── types/                   # ✅ JSON-RPC 类型
    ├── request.go          # 请求类型（延迟参数解析）
    ├── response.go         # 响应类型
    ├── errors.go           # ✅ 错误码定义（含WES自定义）
    └── README.md
```

### **关键文件说明**

| 文件 | 职责 | 实现状态 | 关键特性 |
|------|------|---------|---------|
| **server.go** | JSON-RPC 服务器 | ✅ 完成 | 自动注册 19 个方法 |
| **methods/tx.go** | 交易方法 | ✅ 完成 | ✅ 错误码细化（TX_FEE_TOO_LOW等） |
| **methods/state.go** | 状态查询 | ✅ 完成 | ✅ 状态锚定支持 |
| **types/errors.go** | 错误码定义 | ✅ 完成 | 5种区块链特有错误码 |

---

## 📊 **核心机制**

### **机制1：错误码细化**

**为什么需要**：客户端需要详细错误信息来重试或调整参数

**WES 自定义错误码**：
```go
const (
    CodeServerError         = -32000  // 服务器错误
    CodeUnauthorized        = -32001  // 未授权
    CodeForbidden           = -32002  // 禁止访问
    CodeTxFeeTooLow        = -32003  // ✅ 交易费过低
    CodeTxAlreadyKnown     = -32004  // ✅ 交易已存在
    CodeTxConflicts        = -32005  // ✅ 交易冲突（UTXO双花）
    CodeInvalidSignature   = -32006  // ✅ 无效签名
    CodeInvalidBlockParam  = -32007  // 无效区块参数
    CodeMempoolFull        = -32008  // ✅ 内存池已满
)
```

**错误响应示例**：
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32003,
    "message": "Transaction fee too low",
    "data": {
      "error": "fee rate 1 sat/byte below minimum 5 sat/byte",
      "hint": "Use wes_estimateFee to get recommended fee rate"
    }
  }
}
```

---

### **机制2：状态锚定查询**

**为什么需要**：区块链会重组，查询必须锁定在特定状态

**blockParameter 支持**：
- `"latest"` - 最新状态
- `"earliest"` - 创世区块
- `"pending"` - 包含待处理交易
- `12345` - 特定高度
- `"0xabc..."` - 特定区块哈希

**示例**：
```javascript
// 查询最新余额
web3.eth.getBalance(address, 'latest')

// 查询历史余额（状态锚定）
web3.eth.getBalance(address, 12345)
```

**响应包含状态锚点**：
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "balance": "1000000000000000000",
    "height": 12345,
    "hash": "0xabc...",
    "stateRoot": "0xdef..."
  }
}
```

---

### **机制3：订阅仅在 WebSocket**

**为什么限制**：HTTP 无法主动推送事件

**正确使用**：
```javascript
// ✅ 正确：WebSocket 订阅
const ws = new Web3.providers.WebsocketProvider('ws://localhost:28680');
const web3 = new Web3(ws);

web3.eth.subscribe('newBlockHeaders', (error, result) => {
    if (!error) {
        console.log(result);
    }
});

// ❌ 错误：HTTP 无法订阅
const http = new Web3.providers.HttpProvider('http://localhost:28680');
const web3 = new Web3(http);
web3.eth.subscribe('newBlockHeaders', ...);  // ❌ 失败
```

---

## 🎓 **使用指南**

### **典型场景1：web3.js 调用**

```javascript
const Web3 = require('web3');
const web3 = new Web3('http://localhost:28680');

// 查询最新区块
const blockNumber = await web3.eth.getBlockNumber();
console.log('Latest block:', blockNumber);

// 查询区块详情
const block = await web3.eth.getBlock(blockNumber);
console.log('Block:', block);

// 提交已签名交易
const signedTx = await web3.eth.accounts.signTransaction(tx, privateKey);
const receipt = await web3.eth.sendSignedTransaction(signedTx.rawTransaction);
console.log('TxHash:', receipt.transactionHash);
```

### **直接调用**

```bash
# 查询链ID
curl -X POST http://localhost:28680 \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "wes_chainId",
    "params": [],
    "id": 1
  }'

# 响应
{
  "jsonrpc": "2.0",
  "result": "0x1",
  "id": 1
}
```

---

### **常见误用**

| 误用方式 | 为什么错误 | 正确做法 |
|---------|-----------|---------|
| HTTP 上使用 `wes_subscribe` | HTTP 无法主动推送 | 使用 WebSocket |
| 查询时不带 blockParameter | 重组后数据不一致 | 带上 blockParameter |
| 传递私钥到 `wes_sendRawTransaction` | 违反零信任 | 客户端签名后传 signedTx |
| 忽略错误码 | 无法正确处理错误 | 根据错误码做不同处理 |

---

## ⚠️ **已知限制**

| 限制 | 影响 | 规避方法 | 未来计划 |
|------|------|---------|---------|
| 批量请求未完全实现 | 无法批量调用 | 单次调用 | 📋 支持批量请求 |
| `wes_call` 简化实现 | 无法模拟复杂调用 | 使用本地引擎 | 📋 集成 WASM 引擎 |
| 订阅仅支持 WebSocket | HTTP 无订阅功能 | 使用 WebSocket | ✅ WebSocket 已完整实现 |

---

## 🔍 **设计权衡记录**

### **权衡1：为什么使用 `wes_` 前缀而非 `eth_`？**

**背景**：可以直接使用 `eth_` 前缀兼容 Ethereum

**备选方案**：
1. **wes_ 前缀**：WES 原生方法 - 优势：避免冲突、清晰标识 - 劣势：需客户端适配
2. **eth_ 前缀**：完全兼容 Ethereum - 优势：无缝兼容 - 劣势：可能产生歧义

**选择**：选择了 **wes_ 前缀**

**理由**：
- ✅ WES 使用 UTXO 模型，与 Ethereum 账户模型不同
- ✅ 避免方法语义冲突
- ✅ 保留未来扩展空间

**代价**：客户端需配置方法映射

---

## 📚 **相关文档**

### **内部文档**
- **Methods**：[methods/README.md](./methods/README.md) - 方法实现详解
- **Types**：[types/README.md](./types/README.md) - 类型定义
- **Server**：[server.go](./server.go) - 服务器实现

### **外部规范**
- **JSON-RPC 2.0 规范**：https://www.jsonrpc.org/specification
- **以太坊 JSON-RPC**：https://ethereum.org/en/developers/docs/apis/json-rpc/
- **WES JSON-RPC 完整规范**：[../docs/jsonrpc_spec.md](../docs/jsonrpc_spec.md)

---

## 📋 **文档变更记录**

| 日期 | 变更内容 | 原因 |
|------|---------|------|
| 2025-10-24 | 规范化为模板 v3.0 | 补充错误码、状态锚定、订阅限制 |
| 2025-10-24 | 补充核心机制章节 | 清晰展示3个关键机制 |
| 2025-10-24 | 更新方法列表为19个 | 反映实际实现 |

---

> 📝 **文档说明**
>
> 本文档遵循 `_docs/templates/README_TEMPLATE.md` v3.0 规范
>
> 🎯 **核心理念**：
> - JSON-RPC 2.0 优先：主协议，生态兼容
> - 零信任实现：接收已签名交易
> - 错误码细化：客户端友好的错误处理
> - 状态锚定：保证数据一致性
> - 订阅限制：仅在 WebSocket 上工作
>
> ✅ **实施状态**：
> - 19个核心方法已全部实现
> - 错误码体系已完善
> - 状态锚定已支持
> - 与 web3.js 生态兼容

